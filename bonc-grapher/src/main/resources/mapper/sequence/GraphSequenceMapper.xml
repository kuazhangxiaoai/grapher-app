<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bonc.graph.sequence.mapper.GraphSequenceMapper">

    <resultMap id="BaseResultMap" type="com.bonc.graph.sequence.domain.GraphSequence">
        <id column="sequence_id" property="sequenceId"/>
        <result column="sequence_content" property="sequenceContent"/>
        <result column="article_id" property="articleId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByArticleId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT sequence_id, sequence_content, article_id, create_time, update_time
        FROM graph_sequence
        WHERE article_id = #{articleId}
        ORDER BY create_time DESC
    </select>

    <select id="selectBySequenceId" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT sequence_id, sequence_content, article_id, create_time, update_time
        FROM graph_sequence
        WHERE sequence_id = #{sequenceId}
    </select>

    <insert id="insert" parameterType="com.bonc.graph.sequence.domain.GraphSequence">
        INSERT INTO graph_sequence (
            sequence_id, sequence_content, article_id, create_time, update_time
        ) VALUES (
                     #{sequenceId}, #{sequenceContent}, #{articleId}, #{createTime}, #{updateTime}
                 )
    </insert>

    <update id="updateUpdateTime">
        UPDATE graph_sequence
        SET update_time = #{updateTime}
        WHERE sequence_id = #{sequenceId}
    </update>

    <delete id="deleteBySequenceId" parameterType="java.lang.String">
        DELETE FROM graph_sequence
        WHERE sequence_id = #{sequenceId}
    </delete>

</mapper>